<%- include('partials/header'); -%>

    <h1>Home</h1>

    <p>
        <%= content %>
    </p>

    <% posts.forEach(p=> { %>
        <h2>
            <%= p.title %>
        </h2>
        <p>
            <%= p.content.substring(0,100) + '...' %><a href="/posts/<%= p.title %>">Read More</a>
        </p>

        <% }) %>

            <%- include('partials/footer'); -%>